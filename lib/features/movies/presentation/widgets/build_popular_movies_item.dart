import 'package:flutter/material.dart';

class BuildPopularMovies extends StatelessWidget {
  const BuildPopularMovies({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
